# FlowLuau

C'est une future dinguerie attention les p'tits loups

## Installation

1. Clone the repository
2. Install dependencies using `npm install`
3. Set-up supabase database and update the `.env` file with your database URL
4. Run database migrations using `npx prisma migrate dev --name init`